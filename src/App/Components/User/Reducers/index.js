import User from './Reducers';
import {
  SEND_LOGIN,
  LOGOUT,
  LOGIN_SUCCESSFUL,
  LOGIN_FAILED,
} from './ActionTypes';

const ActionTypes = {
  SEND_LOGIN,
  LOGOUT,
  LOGIN_SUCCESSFUL,
  LOGIN_FAILED,
};

export default User;
export { User, ActionTypes };
